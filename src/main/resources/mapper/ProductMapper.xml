<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.combinatorics.midtermproject.dao.ProductDao">

  <resultMap id="ProductResultMap" type="team.combinatorics.midtermproject.model.po.ProductPO">
    <id column="pid" jdbcType="INTEGER" property="pid" />
    <result column="product_name" jdbcType="VARCHAR" property="productName" />
    <result column="price" jdbcType="DOUBLE" property="price" />
    <result column="sell_time" jdbcType="DATE" property="sellTime" />
    <result column="duration" jdbcType="INTEGER" property="duration" />
    <result column="uid" jdbcType="INTEGER" property="uid" />
  </resultMap>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from product
    where pid = #{pid,jdbcType=INTEGER}
  </delete>

  <insert id="insert" parameterType="team.combinatorics.midtermproject.model.po.ProductPO">
    <selectKey keyProperty="pid" order="AFTER" resultType="java.lang.Integer">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into product (product_name, price, sell_time, 
      duration, `uid`)
    values (#{productName,jdbcType=VARCHAR}, #{price,jdbcType=DOUBLE}, #{sellTime,jdbcType=DATE}, 
      #{duration,jdbcType=INTEGER}, #{uid,jdbcType=INTEGER})
  </insert>

  <update id="updateByPrimaryKey" parameterType="team.combinatorics.midtermproject.model.po.ProductPO">
    update product
    set product_name = #{productName,jdbcType=VARCHAR},
      price = #{price,jdbcType=DOUBLE},
      sell_time = #{sellTime,jdbcType=DATE},
      duration = #{duration,jdbcType=INTEGER},
      `uid` = #{uid,jdbcType=INTEGER}
    where pid = #{pid,jdbcType=INTEGER}
  </update>

  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select pid, product_name, price, sell_time, duration, `uid`
    from product
    where pid = #{pid,jdbcType=INTEGER}
  </select>

  <select id="selectAll" resultMap="BaseResultMap">
    select pid, product_name, price, sell_time, duration, `uid`
    from product
  </select>

</mapper>